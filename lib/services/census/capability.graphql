query CensusCapabilities {
  villageCapabilityEnabled
  animalCensusCapabilityEnabled
}

mutation AnimalCensusCapabilityUpdate($enabled: Boolean!) {
  adminAnimalCensusCapabilityUpdate(enabled: $enabled) {
    enabled
    fields {
      name
      message
    }
  }
}
