query LatestAnimalVillageCensus($villageId: Int!) {
  latestVillageCensusV2(villageId: $villageId, kind: "ANIMAL") {
    id
    censusDate
    submittedAt
    reporter {
      username
    }
    facts {
      rowKey
      rowLabel
      animalQuantity
      householdQuantity
    }
  }
}
