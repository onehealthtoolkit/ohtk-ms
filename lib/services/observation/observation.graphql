query ObservationSubjects(
  $limit: Int!
  $offset: Int!
  $definitionId: Float
  $fromDate: DateTime
  $throughDate: DateTime
) {
  observationSubjects(
    createdAt_Gte: $fromDate
    createdAt_Lte: $throughDate
    definition_Id: $definitionId
    limit: $limit
    offset: $offset
  ) {
    totalCount
    results {
      id
      identity
      title
      description
      createdAt
    }
  }
}

query GetObservationSubjest($id: ID!) {
  observationSubject(id: $id) {
    id
    title
    description
    identity
    formData
    createdAt
    definition {
      name
      description
      registerFormDefinition
    }
    monitoringRecords {
      id
      title
      description
    }
  }
}
