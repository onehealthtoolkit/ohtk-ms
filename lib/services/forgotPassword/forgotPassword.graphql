mutation ResetPasswordRequestCreate($email: String!) {
  resetPasswordRequest(email: $email) {
    success
  }
}

mutation ResetPasswordCreate($token: String!, $password: String!) {
  resetPassword(token: $token, password: $password) {
    success
  }
}
